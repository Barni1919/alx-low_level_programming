#include <stdio.h>

/* 4th project */

/**
 * main - Entry
 *
 * Return: always zero 0 (success)
 */
int main(void)
{

	int x = 0;

	while (x < 10)
	{
		putchar(x);
		x++;
	}
	putchar('\n');

	return (0);
}
